import tensorflow as tf 

class ANN(self,inputSize, hiddenSizese , outputSize):
    def __init__(self):
        self.inputSize = inputSize
        self.hiddenSizese = hiddenSizese
        self.outputSize = outputSize

    def CreatLayer(self):
        layers = {}
        layers["i"] = tf.Variable
        for i in range(hiddenSizese):
            



